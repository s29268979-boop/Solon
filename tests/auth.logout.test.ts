import { describe, expect, it } from "vitest";

// Auth routes removed - app is fully public

// Auth routes removed - app is fully public
describe.skip("auth.logout", () => {
  it("placeholder test - auth removed", async () => {
    expect(true).toBe(true);
  });
});
